<div class="competition">
	<div class="title">{title}</div>
	{#entrants}
	<div class="entrant" id="{id}">
		<div class="name">{name} <span class="user-id">({id})</span></div>
		<div class="score">{current}/{max}</div>
	</div>{~n}
	{/entrants}
</div>